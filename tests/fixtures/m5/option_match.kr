# expect: output 42
(extern "krypton.runtime.KryptonIO" (def println [Object] Unit))
(type Option [a] (| (Some a) (None)))
(def unwrap_or (fn [opt default]
  (match opt
    ((Some x) x)
    ((None) default))))
(def main (fn [] (println (unwrap_or (Some 42) 0))))
