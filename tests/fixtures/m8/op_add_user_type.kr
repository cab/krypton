# expect: output 4
extern "krypton.runtime.KryptonIO" {
    fun println(Object) -> Unit
}

type Vec2 = { x: Int, y: Int }

impl Add[Vec2] {
    fun add(a, b) = Vec2(a.x + b.x, a.y + b.y)
}

fun main() = println(add(Vec2(1, 1), Vec2(3, 2)).x)
