# expect: ok
(type Point (record (x Int) (y Int)))

(trait Eq [a]
  (def eq [a a] Bool))

(impl Eq Point
  (def eq [x y] (if (== (. x x) (. y x)) (== (. x y) (. y y)) false)))

(def main (fn []
  (eq (Point 1 2) (Point 1 2))))
