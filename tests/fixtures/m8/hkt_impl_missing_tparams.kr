# expect: error E0011
type Box[a] = Box(a)

trait Functor[f[_]] {
  fun fmap[a, b](fa: f[a], g: (a) -> b) -> f[b]
}

impl Functor[Box] {
  fun fmap(fa: Box[a], g: (a) -> b) -> Box[b] =
    match fa { Box(x) => Box(g(x)) }
}
