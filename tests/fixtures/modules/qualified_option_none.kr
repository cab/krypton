# skip: js(java-only extern)
# expect: output None
import core/option

extern java "krypton.runtime.KryptonIO" {
  fun println[a](x: a) -> Unit
}

fun main() = {
  let x = option.None
  let y = option.Some(42)
  let _ = match y { Some(_) => 0, None => 0 }
  println(x)
}
