# expect: output "3"
import core/list.{length}

type Point = { x: Int, y: Int }

extern java "krypton.runtime.KryptonIO" {
  fun println[a](x: a) -> Unit
}

fun main() = {
  let p = Point(1, 2)
  let _ = match p { Point { x, y } => x + y }
  println(length(Cons(1, Cons(2, Cons(3, Nil)))))
}
