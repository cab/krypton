# expect: output "hello"
import core/string.{split}
import core/vec.{nth}

extern java "krypton.runtime.KryptonIO" {
  fun println[a](a) -> Unit
}

fun main() = match nth(split("hello world", " "), 0) {
    Some(s) => println(s),
    None => println("error"),
}
