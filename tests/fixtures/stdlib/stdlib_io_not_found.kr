# expect: output "got error"
# skip: js(io)
import core/io.{read_file}

fun handle(r) = match r {
    Ok(_) => println("unexpected"),
    Err(_) => println("got error"),
}

fun main() = handle(read_file("nonexistent_file_xyz.txt"))
