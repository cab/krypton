# expect: output "hello world"
import core/string.{concat, trim}
extern java "krypton.runtime.KryptonIO" { fun println[a](x: a) -> Unit }

fun main() {
    println(trim(concat("  hello", " world  ")));
    0
}
