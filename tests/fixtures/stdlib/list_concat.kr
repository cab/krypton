# expect: output "6"
import core/list.{concat, length}
extern "krypton.runtime.KryptonIO" { fun println(Object) -> Unit }
fun main() = println(length(concat(Cons(1, Cons(2, Cons(3, Nil))), Cons(4, Cons(5, Cons(6, Nil))))))
