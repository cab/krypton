# expect: output "1"
import core/list.{head}
extern java "krypton.runtime.KryptonIO" { fun println[a](a) -> Unit }
fun main() = match head(Cons(1, Cons(2, Cons(3, Nil)))) {
    Some(h) => println(h),
    None => println(0),
}
