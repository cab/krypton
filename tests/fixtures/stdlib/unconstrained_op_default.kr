# expect: error E0312
# Operators on unconstrained type variables require explicit trait bounds.
# Previously this defaulted to Int; now it's an error.
import core/io.{println}

fun add_ints(a, b) = a + b

fun main() = println(show(add_ints(1, 2)))
