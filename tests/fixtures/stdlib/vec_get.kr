# expect: output "1"
import core/vec.{nth}
extern "krypton.runtime.KryptonIO" { fun println(Object) -> Unit }
fun main() = match nth([1, 2, 3], 0) {
    Some(x) => println(x),
    None => println(0),
}
