# skip: js(expected output is jvm-specific)
# expect: output "jvm hello"
@platform([jvm])
fun greet() -> String = "jvm hello"

@platform([js])
fun greet() -> String = "js hello"

fun main() = println(greet())
