# expect: output 99
extern "krypton.runtime.KryptonIO" {
    fun println(Object) -> Unit
}

type Point = { x: Int, y: Int }

fun main() {
    let p = Point(1, 2);
    let p2 = { p | x = 99 };
    println(p2.x)
}
