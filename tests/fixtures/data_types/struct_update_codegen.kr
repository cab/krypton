# expect: output 99
extern java "krypton.runtime.KryptonIO" {
    fun println[a](a) -> Unit
}

type Point = { x: Int, y: Int }

fun main() {
    let p = Point(1, 2);
    let p2 = { p | x = 99 };
    println(p2.x)
}
