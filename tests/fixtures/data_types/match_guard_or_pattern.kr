# expect: output "3\n5\n0"
type Wrapper[a] = Some(a) | Wrap(a)

fun extract(w) = match w {
    Some(x) | Wrap(x) if x > 0 => x,
    _ => 0,
}

fun main() = {
    println(extract(Some(3)));
    println(extract(Wrap(5)));
    println(extract(Some(0)))
}
