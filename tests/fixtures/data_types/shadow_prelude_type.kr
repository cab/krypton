# expect: output "A"
# Local type named `Ok` shadows the prelude Result::Ok variant constructor.
# Verifies that local sum types aren't filtered by type_provenance.

type Ok = A | B

fun main() {
    match A {
        A => println("A"),
        B => println("B")
    }
}
