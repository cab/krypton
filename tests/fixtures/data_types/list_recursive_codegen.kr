# expect: output 20
extern java "krypton.runtime.KryptonIO" {
    fun println[a](a) -> Unit
}

type List[a] = Cons(a, List[a]) | Nil

fun second(xs) = match xs {
    Cons(h, Cons(h2, _)) => h2,
    _ => 0,
}

fun main() = println(second(Cons(10, Cons(20, Nil))))
