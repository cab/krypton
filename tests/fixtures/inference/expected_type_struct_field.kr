# expect: ok
# Locks in `expected_type` threading into struct field construction
# (`infer/expr.rs:1833`). `Nil` is polymorphic `List[a]`; the element
# type is supplied only by the declared field type `List[Int]`.
type Box = { v: List[Int] }

fun mk() -> Box = Box { v = Nil }

fun main() = println(())
