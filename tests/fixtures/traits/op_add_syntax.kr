# expect: output 4
extern java "krypton.runtime.KryptonIO" {
    fun println[a](x: a) -> Unit
}

type Vec2 = { x: Int, y: Int }

impl Semigroup[Vec2] {
    fun combine(a, b) = Vec2(a.x + b.x, a.y + b.y)
}

fun main() = {
    let a = Vec2(1, 1)
    let b = Vec2(3, 2)
    println((a + b).x)
}
