# expect: output "hello 7"
trait Convert[a, b] {
  fun convert(x: a) -> b
}

impl Convert[Int, String] {
  fun convert(x: Int) -> String = "hello " + show(x)
}

fun tag[a, b](x: a) -> b where Convert[a, b] = convert(x)

fun main() = {
  let s: String = tag(7)
  println(s)
}
