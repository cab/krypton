# expect: ok
trait Convert[a, b] {
    fun convert(x: a) -> b
}

impl Convert[Int, String] {
    fun convert(x: Int) -> String = "n"
}

# `b` is unknown at the point of the call; the solver finds a unique
# match by searching with the partially-known type tuple.
fun main() = {
    let s: String = convert(42)
    ()
}
