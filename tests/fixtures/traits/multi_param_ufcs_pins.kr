# expect: ok
trait Convert[a, b] {
    fun convert(x: a) -> b
}

impl Convert[Int, String] {
    fun convert(x: Int) -> String = "n"
}

# UFCS receiver pins position 0 (`a = Int`); the return-type
# annotation pins position 1 (`b = String`).
fun main() = {
    let s: String = 42.convert()
    ()
}
