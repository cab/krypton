# expect: output 4
extern java "krypton.runtime.KryptonIO" {
    fun println(Object) -> Unit
}

type Vec2 = { x: Int, y: Int }

impl Semigroup[Vec2] {
    fun combine(a, b) = Vec2(a.x + b.x, a.y + b.y)
}

fun main() = println(combine(Vec2(1, 1), Vec2(3, 2)).x)
