# expect: output true
extern "krypton.runtime.KryptonIO" {
    fun println(Object) -> Unit
}

type Point = { x: Int, y: Int }

impl Eq[Point] {
    fun eq(x, y) = if x.x == y.x { x.y == y.y } else { false }
}

fun main() = println(eq(Point(1, 2), Point(1, 2)))
