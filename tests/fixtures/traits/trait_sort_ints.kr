# expect: output -1
extern java "krypton.runtime.KryptonIO" {
    fun println(Object) -> Unit
}

trait Cmp[a] {
    fun compare(_0: a, _1: a) -> Int
}

impl Cmp[Int] {
    fun compare(x, y) = if x < y { 0 - 1 } else { if x > y { 1 } else { 0 } }
}

fun main() = println(compare(1, 2))
