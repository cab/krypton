# expect: output true
extern java "krypton.runtime.KryptonIO" {
    fun println[a](a) -> Unit
}

type Box = Box(Int)

impl Show[Box] {
    fun show(_value) = "box"
}

fun main() = println(show(Box(1)) == "box")
