# expect: output true
extern java "krypton.runtime.KryptonIO" {
    fun println[a](a) -> Unit
}

type Option[a] = Some(a) | None

impl Show[Option[a]] where a: Show {
    fun show(_value) = "nested"
}

fun main() = println(show(Some(Some(42))) == "nested")
