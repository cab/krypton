# expect: error E0303
type Point = { x: Int, y: Int }

trait MyEq[a] {
    fun my_eq(_0: a, _1: a) -> Bool
}

trait MyOrd[a] where a: MyEq {
    fun my_compare(_0: a, _1: a) -> Int
}

impl MyOrd[Point] {
    fun my_compare(x, y) = 0
}
