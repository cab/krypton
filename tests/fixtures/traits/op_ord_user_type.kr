# expect: output true

type Wrapper = { val: Int }

impl Eq[Wrapper] {
    fun eq(a, b) = a.val == b.val
}

impl Ord[Wrapper] {
    fun lt(a, b) = a.val < b.val
}

fun main() = println(lt(Wrapper(1), Wrapper(2)))
