# expect: ok
type Option[a] = Some(a) | None

type List[a] = Cons(a, List[a]) | Nil

fun unwrap_nested(opt) = match opt {
    Some(Some(x)) => x,
    Some(None) => 0,
    None => 0,
}

fun first_two(xs) = match xs {
    Cons(h, Cons(h2, _)) => h + h2,
    _ => 0,
}
