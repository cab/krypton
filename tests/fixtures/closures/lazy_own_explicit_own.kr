# expect: ok
# Explicit ~Int annotation but body only uses capture in shared position → still fn
fun use_twice(f: (Int) -> Int) -> Int = f(1) + f(2)

fun test() -> Int {
    let r: ~Int = 2
    let f = (x) -> x + r
    use_twice(f)
}
