# expect: ok
# Captured Own(T) passed to ~T parameter → ~fn, called once = ok
fun consume(buf: ~Option[Int]) -> Option[Int] = match buf {
    Some(n) => Some(n),
    None => None,
}

fun test() -> Option[Int] {
    let buf: ~Option[Int] = Some(0)
    let f = () -> consume(buf)
    f()
}
