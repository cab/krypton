# expect: ok
# Captured Own(T) passed to ~T parameter → ~fn, called once = ok
fun consume(buf: ~Option[Int]) -> Option[Int] = buf

fun test() -> Option[Int] {
    let buf = Some(0)
    let f = () -> consume(buf)
    f()
}
