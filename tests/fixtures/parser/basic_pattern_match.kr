# expect: ok
fun match_wildcard(x) = match x {
    _ => 0,
}

fun match_literal(x) = match x {
    1 => "one",
    2 => "two",
    _ => "other",
}

fun match_constructor(opt) = match opt {
    None => 0,
    Some(x) => x,
}

fun match_tuple(pair) = match pair {
    (a, b) => a + b,
}
