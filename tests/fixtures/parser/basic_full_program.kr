# expect: ok
type Point = { x: Int, y: Int }

type Shape = Circle(Int) | Rect(Int, Int)

fun area(s) = match s {
    Circle(r) => r * r,
    Rect(w, h) => w * h,
}

fun origin() {
    let x = 0;
    x
}

fun main() {
    let p = origin();
    let s = Circle(5);
    println(area(s))
}
