# expect: error E0001
# stdlib peek is shared-bound over bare values; owned values must be borrowed explicitly.
type Choice = A | B
fun classify(s: String) -> Choice = A
fun test(val: ~String) -> String {
    match classify(peek(val)) {
        A => 0,
        B => 0,
    };
    val
}
