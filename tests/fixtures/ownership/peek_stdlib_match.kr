# expect: error E0001
# peek is a bare identity; owned values passed to it are consumed.
type Choice = A | B
fun classify(s: String) -> Choice = A
fun test(val: ~String) -> String {
    match classify(peek(val)) {
        A => 0,
        B => 0,
    };
    val
}
