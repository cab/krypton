# expect: error E0101
type Choice = A | B

fun classify(s: ~String) -> Choice = A

fun consume(s: ~String) -> String = s

fun test(x: ~String) -> String {
    match classify(x) {
        A => "a",
        B => "b",
    };
    consume(x)
}
