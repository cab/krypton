# expect: error E0101
type Callback = { run: ~() -> String }
fun consume(buf: ~String) -> String = match buf { _ => "" }
fun test(x: ~String) -> String {
    let cb: ~Callback = Callback { run = () -> consume(x) }
    (cb.run)()
    (cb.run)()
}
