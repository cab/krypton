# expect: ok
# Recursive function where one branch returns a literal and the recursive call
# returns the accumulator — verifies SCC reconciliation via join_types
fun sum_to(n: Int, acc: Int) -> Int =
    if n == 0 { acc } else { sum_to(n - 1, acc + n) }
