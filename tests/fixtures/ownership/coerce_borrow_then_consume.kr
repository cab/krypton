# expect: error E0101
# blocked: M37-T7 (borrow does not suppress consumption tracking yet)
fun len(&s: ~String) -> Int = 42

fun consume(s: ~String) -> String = match s { _ => "" }

fun test(buf: ~String) -> String {
    len(buf);
    consume(buf)
}
