# expect: error E0101
type Callback = { run: ~() -> String }
fun consume(buf: ~String) -> String = buf
fun test(x: ~String) -> String {
    let cb = Callback { run = () -> consume(x) }
    let f = cb.run
    f()
    f()
}
