# expect: ok
# let x: ~T = owned_call() preserves ownership through the binding
fun consume(x: ~Option[Int]) -> Option[Int] = x

fun test() -> Option[Int] {
    let s: ~Option[Int] = Some(2)
    consume(s)
}
