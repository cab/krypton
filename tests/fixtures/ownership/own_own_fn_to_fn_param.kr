# expect: error E0104
# ~fn does NOT coerce to fn param
fun call_many(f: () -> String) -> String = f()

fun consume(buf: ~String) -> String = match buf { _ => "" }

fun bad(x: ~String) -> String = call_many(() -> consume(x))
