# expect: error E0104
# let-pattern with ~T annotation on non-owned value should be rejected
fun pair(x: String, y: String) -> (String, String) = (x, y)

fun test(x: String, y: String) -> Int {
    let (a, b): ~(String, String) = pair(x, y)
    42
}
