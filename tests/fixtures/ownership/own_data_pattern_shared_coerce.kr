# expect: error E0109
# Pre-M39-T6, bare `Box` in a param position was accepted and the `~String`
# field was coerced. M39-T6 rejects the bare spelling at the annotation
# site.
type Box = { value: ~String }
fun test(b: Box) -> String {
    match b {
        Box { value } => value
    }
}
