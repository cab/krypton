# expect: error E0101
# blocked: M37-T7 (borrow does not suppress consumption tracking yet)
fun len(&s: ~String) -> Int = 42
fun consume(s: ~String) -> Int = match s { _ => 0 }

fun test(buf: ~String) -> Int {
    len(buf);
    len(buf);
    consume(buf)
}
