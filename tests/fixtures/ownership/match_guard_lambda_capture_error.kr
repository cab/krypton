# expect: error E0104
type Option[a] = Some(a) | None

fun consume(buf: ~String) -> Int = 1
fun peek[a](x: a) -> a where a: shared = x

fun bad(x: ~String) -> Int =
    peek(() -> match Some(0) {
        Some(n) if consume(x) > 0 => n,
        Some(n) => n,
        None => 0,
    })()
