# expect: ok
fun consume(buf: ~Option[Int]) -> Option[Int] = match buf {
    Some(n) => Some(n),
    None => None,
}

fun test() -> Option[Int] =
    consume({
        let _ = 1
        Some(0)
    })
