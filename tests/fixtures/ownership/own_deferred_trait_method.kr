# expect: ok
# trait method with owned return type — is_owned_expr must recognize non-constructor calls
trait Default[a] {
    fun default() -> ~a
}

impl Default[Option[Int]] {
    fun default() -> ~Option[Int] = Some(0)
}

fun test() -> ~Option[Int] {
    let x = default[Option[Int]]()
    x
}
