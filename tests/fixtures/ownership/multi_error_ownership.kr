# expect: error E0101
# expect: error E0101
fun consume(buf: ~String) -> String = match buf { _ => "" }

fun bad1(x: ~String) -> String {
    consume(x);
    consume(x)
}

fun bad2(y: ~String) -> String {
    consume(y);
    consume(y)
}
