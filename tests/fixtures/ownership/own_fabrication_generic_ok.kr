# expect: ok
# Owned arg through generic instantiation — should pass
fun ok[T](f: (T) -> String, s: T) -> String = f(s)

fun test() -> String {
    let lam: (~Option[Int]) -> String = a -> "hi"
    ok(lam, Some(42))
}
