# expect: ok
type Wrapper = { action: ~() -> String }
fun consume(buf: ~String) -> String = buf
fun test(x: ~String) -> String {
    let w = Wrapper { action = () -> consume(x) }
    match w {
        Wrapper { action } => action()
    }
}
