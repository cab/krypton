# expect: ok
fun consume(buf: ~String) -> String = match buf { _ => "" }
fun test(x: ~String) -> String {
    let funcs: ~List[~() -> String] = Cons(() -> consume(x), Nil)
    match funcs {
        Cons(f, _) => f(),
        Nil => "empty",
    }
}
