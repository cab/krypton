# expect: ok
type Buf = { name: String, data: ~String }

fun consume(b: ~Buf) -> String = match b { Buf { name, data: _ } => name }

fun test(s: ~Buf, d: ~String) -> String {
    let s2 = { s | data = d };
    consume(s2)
}
