# expect: output "done"
extern java "krypton.runtime.KryptonIO" {
    fun println[a](x: a) -> Unit
}

type Box = MkBox(Int)

fun main() -> Unit = {
    let b: ~Box = MkBox(1)
    let b: ~Box = MkBox(2)
    println("done")
}
