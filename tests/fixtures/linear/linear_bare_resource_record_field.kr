# expect: error E0109
# A record field carrying a resource-carrying type must be `~Conn`.
extern java "net.Sock" as pub Sock lifts always {}
extern js "net.mjs"   as pub Sock lifts always {}

type Conn = { sock: ~Sock }

type Wrap = { inner: Conn }

fun main() -> Unit = ()
