# expect: ok
# Borrow mode parameters expose ownership via `&` — the bare type
# spelling is legal inside the borrow mode.
type Conn = { id: ~Int }

fun peek(&c: ~Conn) -> Int = 1

fun main() -> Unit = ()
