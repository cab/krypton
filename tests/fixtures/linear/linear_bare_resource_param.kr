# expect: error E0109
# Consume-mode param with a bare resource-carrying type must be `~Conn`.
extern java "net.Sock" as pub Sock lifts always {}
extern js "net.mjs"   as pub Sock lifts always {}

type Conn = { sock: ~Sock }

fun take(c: Conn) -> Int = 1

fun main() -> Unit = ()
