# expect: ok
type Choice = A | B

fun peek(s: String) -> Choice = A

fun consume(s: ~String) -> String = s

fun test(val: ~String) -> String {
    match peek(val) {
        A => 0,
        B => 0,
    };
    consume(val)
}
