# expect: error E0102
type Option[a] = Some(a) | None

fun consume(buf: ~String) -> Int = 42

fun test(x: ~String, opt: Option[Int]) -> String {
    match opt {
        Some(_) => consume(x),
        None => 0,
    };
    x
}
