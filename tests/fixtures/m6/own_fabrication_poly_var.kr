# expect: error E0101
# Inferred ownership: `a` is inferred as ~String from consume's ~T param,
# so using it twice (consume + borrow) is a double-use error.
fun consume[T](x: ~T) -> T = x
fun borrow(x: String) -> String = x

fun test(a) {
    let x = consume(a)
    let y = borrow(a)
    y
}
