# expect: ok
# All-owned match arms preserve ownership through pair-wise joining
type Color = Red | Green | Blue

fun consume(buf: ~String) -> String = buf

fun test(c: Color) -> String =
    consume(match c {
        Red => "red",
        Green => "green",
        Blue => "blue",
    })
