# expect: error E0101
type Option[a] = Some(a) | None

fun consume(x: ~Option[String]) -> String = "done"

fun test(x: ~Option[String]) -> String {
    match x {
        Some(s) => s,
        None => "none",
    };
    consume(x)
}
