# expect: error E0001
# Bare T passed to ~T param should be rejected (ownership fabrication)
fun consume(buf: ~String) -> String = buf

fun borrow(s: String) -> String = s

fun test(x: String) -> String = consume(x)
