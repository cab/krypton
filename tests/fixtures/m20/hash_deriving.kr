# expect: ok
# expect: output "true\ntrue"
type Point = { x: Int, y: Int } deriving (Hash, Eq)
fun main() = {
  let a = Point { x = 1, y = 2 }
  let b = Point { x = 1, y = 2 }
  let c = Point { x = 3, y = 4 }
  println(hash(a) == hash(b))
  println(hash(a) != hash(c))
}
