# expect: output 15
extern java "krypton.runtime.KryptonIO" {
    fun println[a](a) -> Unit
}

fun main() -> Unit = {
  let t = (1, 2, 3, 4, 5)
  let (a, b, c, d, e) = t
  println(a + b + c + d + e)
}
