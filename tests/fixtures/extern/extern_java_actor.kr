# expect: ok
import core/actor.{Mailbox, Ref, send, receive, spawn, actor_ref}

fun echo(mb: Mailbox[Int]) {
  let msg = receive(mb)
  let me = actor_ref(mb)
  send(me, msg)
}

fun use_ref(r: Ref[Int]) -> Unit = send(r, 42)

fun main() = echo
