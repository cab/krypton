# expect: error E0104
# blocked: tuple expected-type propagation (elements inferred without Own hint)
extern java "krypton.runtime.KryptonIO" {
    fun println[a](x: a) -> Unit
}

type Handle = MkHandle(String)

impl Disposable[Handle] {
    fun dispose(h: ~Handle) -> Unit = println("closed")
}

fun make_pair() -> (Int, ~Handle) = (1, MkHandle("test"))

fun main() -> Unit = {
    let (n, h) = make_pair()
    println("opened")
}
