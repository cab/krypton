import core/actor.{Mailbox, Ref, spawn, receive, send}

type Msg = Ping(String)

fun printer(mb: Mailbox[Msg]) -> Unit = match receive(mb) {
  Ping(text) => println(text),
}

fun main() {
  let ref = spawn(printer)
  send(ref, Ping("hello from another actor"))
}
