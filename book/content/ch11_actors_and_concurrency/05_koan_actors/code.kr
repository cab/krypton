import core/actor.{Mailbox, Ref, receive, send}

type Msg = Ping(Ref[String])

fun responder(mb: Mailbox[Msg]) -> Unit = match receive(mb) {
  Ping(reply) => send(reply, ???),
}

fun main() {
  # TODO: make the actor reply with a string.
  println("finish the responder actor")
}
