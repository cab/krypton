fun describe(opt: Option[Int]) -> String = match opt {
  Some(value) if value > 0 => "positive",
  Some(0) => "zero",
  Some(value) => "negative: " + show(value),
  None => "missing",
}

fun main() {
  println(describe(Some(-2)))
  println(describe(Some(4)))
}
