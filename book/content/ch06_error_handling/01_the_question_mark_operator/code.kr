import core/string.{parse_int}

fun read_port(text: String) -> Option[Int] = {
  let port = parse_int(text)?
  Some(port + 1)
}

fun main() {
  println(read_port("8080"))
  println(read_port("nope"))
}
