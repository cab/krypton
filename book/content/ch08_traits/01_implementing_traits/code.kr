trait Area[a] {
  fun area(x: a) -> Int
}

type Square = { size: Int }

impl Area[Square] {
  fun area(square: Square) -> Int = square.size * square.size
}

fun main() {
  println(area(Square { size = 4 }))
}
