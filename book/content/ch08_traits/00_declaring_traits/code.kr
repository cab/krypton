trait Label[a] {
  fun label(x: a) -> String
}

type Status = Ready | Waiting

impl Label[Status] {
  fun label(status: Status) -> String = match status {
    Ready => "ready",
    Waiting => "waiting",
  }
}

fun main() {
  println(label(Ready))
}
