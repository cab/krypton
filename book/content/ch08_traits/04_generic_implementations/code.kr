type Box[a] = Box(a)

impl[a] Show[Box[a]] where a: Show {
  fun show(box: Box[a]) -> String = match box {
    Box(value) => "Box(" + show(value) + ")",
  }
}

fun main() {
  println(show(Box(42)))
}
