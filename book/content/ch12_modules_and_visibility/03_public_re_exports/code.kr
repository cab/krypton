# colors.kr
pub type Color = Red | Green | Blue
pub fun default_color() -> Color = Green

# palette.kr
pub import colors.{Color, Red, Green, Blue, default_color}

# main.kr
import palette.{default_color}

fun main() {
  println(default_color())
}
