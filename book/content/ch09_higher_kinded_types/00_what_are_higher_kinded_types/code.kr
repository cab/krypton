type Box[a] = Box(a)

trait Keep[f[_]] {
  fun keep[a](value: f[a]) -> f[a]
}

impl Keep[Box] {
  fun keep[a](value: Box[a]) -> Box[a] = value
}

fun main() {
  println("Higher-kinded parameters talk about type constructors like Box.")
}
