type Box[a] = Box(a)

trait Functor[f[_]] {
  fun fmap[a, b](fa: f[a], g: (a) -> b) -> f[b]
}

impl Functor[Box] {
  fun fmap[a, b](fa: Box[a], g: (a) -> b) -> Box[b] = ???
}

fun main() {
  # TODO: make fmap apply g to the value inside Box.
  println("Fill in fmap for Box.")
}
